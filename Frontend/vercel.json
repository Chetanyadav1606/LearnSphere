{
    "buildCommand": "npm run build",
    "outputDirectory": "dist/book-network-ui/browser",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/.*",
            "dest": "/index.html"
        }
    ]
}